{% extends "emails/base.txt" %}

{% block content %}{% if role == 'coach' %}NEW SESSION BOOKED{% else %}YOUR SESSION IS CONFIRMED{% endif %}

Hi {{ recipient_name }},
{% if role == 'coach' %}{{ other_name }} has booked a session with you.{% else %}Your session with {{ other_name }} is confirmed. We look forward to seeing you!{% endif %}

  Session  : {{ skill_name }}
  When     : {{ session_when }}
  Duration : {{ duration }} minutes
  {% if role == 'coach' %}Client{% else %}Coach{% endif %}    : {{ other_name }}

When it's time, join the session here:
{{ join_link }}

({% if role == 'coach' %}Or view in My Sessions{% else %}Or view in My Learning{% endif %}: {{ manage_url }})

We'll send you reminders before the session so it never sneaks up on you.{% endblock %}
